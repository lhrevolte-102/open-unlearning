# @package eval.tofu.metrics.retain_Q_A_token_Prob
defaults:
  - ../../data/datasets@datasets: TOFU_QA_retain_eval
  - ../../collator@collators: DataCollatorForSupervisedDatasetwithIndex

handler: token_probability
batch_size: ${eval.tofu.batch_size}

datasets:
  TOFU_QA_retain_eval:
    args:
      hf_args:
        name: ${eval.tofu.retain_split}
      question_key: ${eval.tofu.question_key}
