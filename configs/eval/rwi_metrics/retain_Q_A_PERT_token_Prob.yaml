# @package eval.rwi.metrics.retain_Q_A_PERT_token_Prob
defaults:
  - ../../data/datasets@datasets: RWI_QA_retain_pert
  - ../../collator@collators: DataCollatorForSupervisedDatasetwithIndex

handler: token_probability
batch_size: ${eval.rwi.batch_size}

datasets:
  RWI_QA_retain_pert:
    args:
      hf_args:
        name: retain_perturbed
