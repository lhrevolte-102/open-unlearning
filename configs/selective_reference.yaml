defaults:
  - model: Llama-3.2-1B-Instruct
  - collator: DataCollatorForSupervisedDatasetwithIndex
  - hydra: default
  - paths: default
  - _self_
  - experiment: null

seed: 0
mode: selective_reference
task_name: selective_reference
trainer:
  handler: selective_reference
method: NPO
num_repeats: ???
repeat_split_seed: 0
retain_split: retain90
reference_splits_output_dir: ${paths.output_dir}/reference_splits
reference_splits_summary_path: ${paths.output_dir}/reference_splits/reference_splits.json
checkpoint_root_dir: ${paths.output_dir}/models
reference_manifest_output_path: ${paths.output_dir}/reference_models.json
validate_checkpoint_paths: false

data: ???
