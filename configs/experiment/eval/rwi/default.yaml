# @package _global_

defaults:
  - override /model: Llama-3.2-3B-Instruct
  - override /eval: rwi

model:
  model_args:
    pretrained_model_name_or_path: ../models/Llama-3.2-3B-Instruct
  tokenizer_args:
    pretrained_model_name_or_path: ../models/Llama-3.2-3B-Instruct
  template_args:
    condition: baseline

task_name: ???
